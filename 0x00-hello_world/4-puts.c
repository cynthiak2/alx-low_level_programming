#include <stdio.h>
/**
 * main - Entry point
 * Prints a string
 * return (0)
 */
int main(void)
{
	char *msg = "\"Programming is like building a multilingual puzzle";

	puts(msg);
	/* Return 0 for successful completion */
	return (0);
}
