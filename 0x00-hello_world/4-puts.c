#include <stdio.h>
/**
 * main - Entry point
 * Prints a string
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *msg = "\"Programming is like building a multilingual puzzle";

	puts(msg);	
	return (0);
}
